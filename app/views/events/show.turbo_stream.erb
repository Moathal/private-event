<%= turbo_stream.replace "page_content", template: "events/show", locals: { event: @event, attendance: @attendance, unattending_users: @unattending_users, attend: @attend } %>
<%= turbo_stream.replace "notifications", partial: "layouts/notifications", locals: { notifications: @notifications, unread: @unread, read: @read } %>
<%= turbo_stream.replace "count", partial: "layouts/count", locals: { unread: @unread } %>